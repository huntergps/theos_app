/// Invoice datasources barrel export
///
/// Invoice local data access is now handled by generated managers
/// (accountMoveManager, accountMoveLineManager) and InvoiceRepository.
/// This barrel is kept empty for backward compatibility.
library;
