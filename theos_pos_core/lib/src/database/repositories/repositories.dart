/// Core repositories module - exports base and common repository classes
///
/// Pure Dart repository implementations.
/// Riverpod providers are defined in the main app (theos_pos).
library;

// Base
export 'base_repository.dart';

// Note: CommonRepository lives in theos_pos (has Flutter/Riverpod deps)
// Note: repository_providers.dart stays in theos_pos (has Flutter/Riverpod deps)
