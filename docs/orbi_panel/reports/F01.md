# F01 · Revisión del integrador

Fecha: 2026-09-06. Resultado: **aprobado**.

## Resultado observable

- `theos_panel` existe como aplicación Flutter independiente para seis shells,
  con nombre visible Orbi ERP e identificadores técnicos propios.
- La app consume `orbi_runtime` y resuelve Material oficial, Riverpod,
  `go_router` y `reactive_forms` sin `theos_pos`, Fluent ni `odoo_widgets`.
- `orbi_runtime` materializa C01/C05 sin fingir conexión, sesión o sync exitosos.
- Makefile y CI contienen análisis, pruebas y builds de scaffold por host.

## Evidencia del integrador

- `make analyze-orbi` — correcto, cero hallazgos en ambos paquetes.
- `make test-orbi` — correcto: runtime 8 pruebas; app 1 prueba.
- `make build-orbi-web` — correcto; produjo `theos_panel/build/web/index.html`.
- `flutter pub deps --style=compact` + `rg` — sin dependencias prohibidas.
- `git diff --check` y validación YAML — correctos.

## Límites conservados

Esto acredita el scaffold y contratos iniciales, no autenticación ERP2, flujos de
venta, notificaciones, operación offline ni soporte funcional de seis plataformas.
Esas garantías pertenecen a tareas posteriores. No se modificó Odoo.
