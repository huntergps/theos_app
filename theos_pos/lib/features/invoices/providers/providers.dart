/// Invoice provider barrel.
///
/// Repository providers live in the shared repository composition layer and
/// order-scoped streams are declared next to the invoice widgets that consume
/// them. This library remains as the stable feature export point.
library;
