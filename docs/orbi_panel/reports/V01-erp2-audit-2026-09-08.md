# V01 · auditoría de preparación ERP2

Estado: `blocked` antes de cualquier conexión o mutación.

## Revisiones de referencia

- App: `HEAD efff735`.
- Checkout Odoo desplegable: `c715abba8`.
- Guardas, clasificación y secuencia revisadas en `theos_panel/lib/erp2_harness.dart`.
- Integración opt-in revisada en `theos_panel/integration_test/orbi_erp2_test.dart`.

## Variables faltantes

El entorno de esta ejecución no contiene ninguna variable V01: destino,
base, modo, ambos sentinels, prefijo, cleanup, evidencia, API key read-only,
los cuatro actores (`SELLER`, `CASHIER`, `SUPERVISOR`, `WAREHOUSE`) con login,
ID y API key, las cuatro fixtures (`CASH`, `CREDIT`, `MIXED`, `FSC`) con ID y
referencia, aprobaciones comerciales (incluida la comercial FSC),
`FSC_APPROVAL_ID`, `FSC_PAYMENT_WIZARD_ID`, `MIXED_PAYMENT_WIZARD_ID`,
`MIXED_CASH_AMOUNT`, y sesión/diario/método de contado.

Por tanto no existe identidad real independiente, fixture prefijada ni
autorización de lectura para abrir ERP2. No se usó `admin`, `newerp` ni una
cuenta sustituta.

## Evidencia ejecutada

| Comando | Resultado |
| --- | --- |
| `python3 scripts/check_orbi_plan.py --ready` | PASS; estructura OK, sin tareas pendientes ejecutables |
| `flutter analyze lib/erp2_harness.dart test/erp2_harness_test.dart integration_test/orbi_erp2_test.dart` | PASS |
| `flutter test test/erp2_harness_test.dart` | PASS; 5 pruebas |
| `git diff --check` | PASS |
| `flutter test integration_test/orbi_erp2_test.dart` | No cuenta como ERP2: el runner intentó build iOS local y se terminó; no contactó servidor |

No se ejecutó `scripts/verify_orbi_erp2.py`: sin `ERP2_URL`/`ERP2_API_KEY`
solo habría producido un fallo local y escrito su evidencia por defecto.
No se ejecutó el stage remoto ni una escritura.

## Bloque mínimo reproducible

Con el entorno actual, `Erp2HarnessConfig.fromEnvironment(Platform.environment)`
no puede satisfacer `targetErrors()`/`writeErrors()`: faltan destino exacto,
consentimientos, actores, cuatro fixtures y dependencias de cobro/aprobación.
La integración permanece omitida porque falta
`ORBI_ERP2_RUN_WRITES=I_UNDERSTAND_ORBI_E2E_WRITES`; aun con ese sentinel,
`ORBI_ERP2_AUDIT_API_KEY` y el resto del contrato deben estar presentes antes
del preflight server-side.
